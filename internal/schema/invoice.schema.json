{
  "type": "object",
  "required": ["id", "currency", "lines", "net", "tax", "gross"],
  "properties": {
    "id": {"type": "string"},
    "currency": {"type": "string"},
    "lines": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["description", "quantity", "unit_price", "tax_rate_bp"],
        "properties": {
          "description": {"type": "string"},
          "quantity": {"type": "integer"},
          "unit_price": {"type": "integer"},
          "tax_rate_bp": {"type": "integer"}
        }
      }
    },
    "net": {"type": "integer"},
    "tax": {"type": "integer"},
    "gross": {"type": "integer"}
  }
}
